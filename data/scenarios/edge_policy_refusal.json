{
  "id": "EDGE-POLICY-REFUSAL",
  "name": "Edge case: policy refusal via required-approver-role, isolated from blast radius (BR=1, below the 5 threshold) — distinct rule from SCEN-03/07's blast-radius block",
  "workflow_type": "incident",
  "is_edge_case": true,
  "ci_id": "CI-0007",
  "alert_category": "fault",
  "auto_approve": true,
  "expected": {
    "status": ["pending_approval", "blocked", "complete"],
    "policy_note": "REQUIRED_APPROVER_ROLE should trigger (prod P1, actor_role=operator is not sre_lead/change_manager) even though blast radius (1) is well under both thresholds"
  }
}
