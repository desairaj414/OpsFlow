# Data Provenance

Every dataset here is synthetic, generated by our own scripts. No scraped data, no real ticket
exports, no customer names — see `.knowledge/citations.md` for the full provenance statement.

| Dataset | Generator | Generated | Schema rationale |
|---|---|---|---|
| `cmdb.json` (CIs + relationships) | `backend/data_gen/cmdb.py` (seed=20260807) | 2026-08-07 | Field names/shape modelled on publicly documented CMDB concepts (id/type/owner/environment/criticality/patch_level), per `schema-db.md` `cmdb_ci`. No real vendor schema copied verbatim. |
| `cmdb_ground_truth.json` | `backend/data_gen/cmdb.py` (seed=20260807) | 2026-08-07 | Same shape as `cmdb.json`, ~35% of records deliberately diverged in one field (patch_level/criticality/environment/owner) — models real-world CMDB drift for the Drift-vs-Truth screen (PRD §6.1). |
| `failed_remediations.json` | `backend/data_gen/cmdb.py` (seed=20260807) | 2026-08-07 | Seeds `negative_kb_entries` (`schema-db.md`) — synthetic tried-and-failed fixes, not derived from any real incident data. |
| `alerts.json` | `backend/data_gen/alerts.py` (seed=20260807) | 2026-08-07 | 3 raw shapes (Prometheus-like, SNMP-terse, APM-verbose) deliberately mixed so intake normalisation has to handle real heterogeneity. Field names modelled on publicly documented monitoring tool conventions, not copied from any real vendor payload. |
| `metrics/*.csv`, `metrics_index.json` | `backend/data_gen/metrics.py` (seed=20260807) | 2026-08-07 | 40 CIs sampled, 10 given a deliberate slow-degradation curve (memory/latency creep) — models the "sustained metric improvement across a window" verification criterion for the Performance Tuning workflow (domain-workflows.md). |
| `tickets.csv`, `change_calendar.json`, `patch_inventory.csv` | `backend/data_gen/tickets.py` (seed=20260807) | 2026-08-07 | Synthetic ITSM-adjacent history covering all 3 workflow families (incident/change/tuning). No real ticket exports, per the provenance statement above. |
| `runbooks/RB-*.md` (22), `postmortems/PM-*.md` (9) | `backend/data_gen/runbooks.py` (seed=20260807) | 2026-08-07 | Numbered, clause-structured, declared human-step-count per PRD §6.4. Includes the one deliberate page-break trap case (`RB-001.md`, step 4) required by `domain-guardrails.md` chunking spec. **PDF subset (2-3 runbooks as PDF, per PRD §6.1) not yet generated** — deferred, needs a PDF-writing dependency not yet in `requirements.txt`; tracked in `state-progress.md` MOCKED & DEFERRED. |
| `demo_outputs.json` | `scripts/pregenerate_demo_outputs.py` | 2026-08-13 | Instant Demo mode's fixed dataset (public-hosting-ready refactor) — the real agent pipeline run once, live, against a real Gemini key (Free Demo Key provider), for all 6 named scenarios (`data/scenarios/SCEN-01..06.json`), keyed by `ci_id:workflow_type`. Zero live model calls happen at Instant-Demo request time; this file is what's replayed instead. **The 7th planned fixture (`image_pii_demo`, a PII-scrubbing showcase through the real, unmodified vision-intake pipeline) is not yet included** — Gemini's vision models returned a sustained "high demand" 503 across both `gemini-flash-latest` and `gemini-flash-lite-latest` during generation (confirmed external/transient via the API's own error message, not a quota or code issue). `scripts/pregenerate_demo_outputs.py` is idempotent — rerun it once Gemini vision capacity recovers; it will skip the 6 already-saved scenarios and only retry this one entry. |
